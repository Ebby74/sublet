---
phase: 03-financial-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/payment/receipt-view.tsx
  - src/app/payments/[id]/receipt/page.tsx
autonomous: true
requirements:
  - FIN-03
user_setup: []
must_haves:
  truths:
    - User can view receipt for any payment
    - User can print receipt
    - User can download receipt as PDF
    - Receipt is styled for A4 printing
  artifacts:
    - src/components/payment/receipt-view.tsx
    - src/app/payments/[id]/receipt/page.tsx
  key_links:
    - receipt -> payment detail (fetched via API)
    - receipt -> tenant details (from linked tenant)
    - receipt -> property details (from lease -> property)
---

<objective>
Receipt Generation — Generate HTML and PDF receipts for payments.
</objective>

<context>
@.planning/phases/03-financial-core/03-CONTEXT.md

**Phase goal:** Track payments, generate invoices, provide financial overview with Excel export.

**Requirements addressed:**
- FIN-03: Generate invoice/receipt for payments

**Key decisions (from CONTEXT.md):**
- D-10: HTML receipt for quick view (styled, printable)
- D-11: PDF download for official record
- D-12: Receipt contains: Tenant name, property, amount (MYR), date, description, reference number
- D-13: Receipt auto-generates reference number (format: RCP-YYYYMMDD-XXXX)

**Receipt structure:**
- Header: Company/Owner name, Logo area
- Receipt number + date
- From: Property owner details
- To: Tenant name + IC number (for LHDN)
- Itemized list: Description, Amount (MYR)
- Total
- Footer: Payment method (Cash/Transfer), Bank details (if transfer), Terms
</context>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<must_haves>
truths:
  - User can view receipt for any payment
  - User can print receipt
  - User can download receipt as PDF
  - Receipt is styled for A4 printing
artifacts:
  - src/components/payment/receipt-view.tsx
  - src/app/payments/[id]/receipt/page.tsx
key_links:
  - receipt -> payment detail (fetched via API)
  - receipt -> tenant details (from linked tenant)
  - receipt -> property details (from lease -> property)
</must_haves>

<tasks>

<task type="auto">
  <name>task 1: Create receipt component</name>
  <files>src/components/payment/receipt-view.tsx</files>
  <read_first>
- src/services/payment-service.ts (to be created in 03-01)
- src/services/tenant-service.ts
- src/services/lease-service.ts
  </read_first>
  <action>
**Create `src/components/payment/receipt-view.tsx`:**

1. Props: paymentId, mode ('html' | 'pdf')
2. Fetches payment with tenant and lease
3. Generates styled HTML:
   - A4-optimized layout (210mm width)
   - Company header area (owner name from user)
   - Receipt title: "RECEIPT" / "INVOICE"
   - Reference number (from payment.referenceNumber)
   - Date issued
   - Bill to: Tenant name, IC (from tenant.icNumber), Property address
   - Item table:
     | Description | Amount |
     |------------|-------|
     | [payment description/type] | [MYR amount] |
   - Total amount (bold, larger)
   - Payment status badge
   - Footer: "Thank you for your payment" / bank details for transfer
4. Print styles via @media print
5. For PDF: Use react-pdf or @react-pdf/renderer approach
   - Note: PDF generation deferred to library selection during execution
  </action>
  <verify>
<automated>test -f src/components/payment/receipt-view.tsx</automated>
  </verify>
  <done>
- Receipt displays all payment details
- Receipt is print-styled (A4)
- Receipt formatted per D-12, D-13
  </done>
</task>

<task type="auto">
  <name>task 2: Create receipt page route</name>
  <files>src/app/payments/[id]/receipt/page.tsx</files>
  <read_first>
- src/app/payments/[id]/page.tsx (reference detail page)
  </read_first>
  <action>
**Create `src/app/payments/[id]/receipt/page.tsx`:**

1. Server component that fetches payment by ID
2. Renders receipt-view component
3. Actions in header:
   - "Print" button (window.print())
   - "Download PDF" button (generates PDF)
   - "Back to Payment" link
4. Print-optimized layout (hide navigation, full-width receipt)
5. If payment not found: 404 page

**Print handling:**
- CSS: @media print { body * { visibility: hidden; } .receipt-view, .receipt-view * { visibility: visible; } }
- or use print-specific CSS classes
  </action>
  <verify>
<automated>test -f "src/app/payments/[id]/receipt/page.tsx"</automated>
  </verify>
  <done>
- Receipt page at /payments/[id]/receipt
- Print button works
- PDF download available
  </done>
</task>

</tasks>

<verification>
- [ ] Receipt displays reference, tenant, amount, date correctly
- [ ] Print styling hides UI chrome
- [ ] PDF download generates valid PDF
</verification>

<success_criteria>
User can view, print, and download receipts for all payments in proper format.
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-core/03-01-SUMMARY.md`
</output>