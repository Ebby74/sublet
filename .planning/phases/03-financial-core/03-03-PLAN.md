---
phase: 03-financial-core
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/components/dashboard/financial-summary.tsx
  - src/components/dashboard/income-expense-chart.tsx
  - src/components/dashboard/outstanding-list.tsx
autonomous: true
requirements:
  - FIN-04
  - FIN-05
user_setup: []
must_haves:
  truths:
    - Dashboard shows income vs expenses bar chart by month
    - Dashboard shows summary cards with totals
    - Dashboard shows outstanding payments with color-coded alerts
    - User can filter by time period
    - Dashboard updates when payments change
  artifacts:
    - src/app/(dashboard)/page.tsx (updated)
    - src/components/dashboard/financial-summary.tsx
    - src/components/dashboard/income-expense-chart.tsx
    - src/components/dashboard/outstanding-list.tsx
  key_links:
    - dashboard -> payment-service (fetches payment data)
    - summary -> payment stats (aggregated by type)
    - chart -> monthly payment aggregation
    - outstanding -> payments with pending/overdue status
---

<objective>
Financial Dashboard — Dashboard with income/expense charts, summary cards, and outstanding payments view.
</objective>

<context>
@.planning/phases/03-financial-core/03-CONTEXT.md
@src/app/(dashboard)/page.tsx

**Phase goal:** Track payments, generate invoices, provide financial overview with Excel export.

**Requirements addressed:**
- FIN-04: Dashboard showing monthly income/expenses chart
- FIN-05: Outstanding payments view

**Key decisions (from CONTEXT.md):**
- D-14: Combined view: bar chart (income vs expenses by month) + summary cards
- D-15: Summary cards: Total Income, Total Expenses, Net Profit, Outstanding Amount
- D-16: Outstanding payments section with color-coded alerts
- D-17: Inline highlighting of unpaid items in transactions list
- D-18: Time period filter: This Month, Last Month, This Year, Custom Range

**Chart decisions (OpenCode's Discretion):**
- Library: Recharts, Chart.js, or native — choose during execution

**Outstanding alerts (D-33, D-34, D-35):**
- Yellow: Due within 3 days
- Orange: 1-3 days overdue
- Red: 4+ days overdue
</context>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<must_haves>
truths:
  - Dashboard shows income vs expenses bar chart by month
  - Dashboard shows summary cards with totals
  - Dashboard shows outstanding payments with color-coded alerts
  - User can filter by time period
  - Dashboard updates when payments change
artifacts:
  - src/app/(dashboard)/page.tsx (updated)
  - src/components/dashboard/financial-summary.tsx
  - src/components/dashboard/income-expense-chart.tsx
  - src/components/dashboard/outstanding-list.tsx
key_links:
  - dashboard -> payment-service (fetches payment data)
  - summary -> payment stats (aggregated by type)
  - chart -> monthly payment aggregation
  - outstanding -> payments with pending/overdue status
</must_haves>

<tasks>

<task type="auto">
  <name>task 1: Create dashboard summary and chart components</name>
  <files>src/components/dashboard/financial-summary.tsx, src/components/dashboard/income-expense-chart.tsx</files>
  <read_first>
- src/app/(dashboard)/page.tsx (existing placeholders)
- src/lib/format.ts (currency formatting)
  </read_first>
  <action>
**Create `src/components/dashboard/financial-summary.tsx`:**

1. Props: userId, period (this-month | last-month | this-year | custom)
2. Fetch aggregated stats from payment-service
3. Display 4 summary cards:
   - Total Income (green): All income payments sum
   - Total Expenses (red): All expense payments sum
   - Net Profit (blue): Income - Expenses
   - Outstanding (yellow): Sum of pending + overdue
4. Each card shows: Icon, Label, Amount (formatted in MYR), +/- change vs previous period
5. Loading skeleton while fetching

**Create `src/components/dashboard/income-expense-chart.tsx`:**

1. Props: userId, period
2. Use recharts (or chosen library):
   - Bar chart: X-axis = months, Y-axis = amount
   - Two bars per month: Income (green), Expenses (red)
   - 6 or 12 month view based on period
3. Tooltip: Shows exact amounts on hover
4. Legend: Toggle income/expense visibility
5. ResponsiveContainer for mobile
  </action>
  <verify>
<automated>test -f src/components/dashboard/financial-summary.tsx</automated>
  </verify>
  <done>
- Summary cards show correct totals
- Chart displays income vs expenses by month
  </done>
</task>

<task type="auto">
  <name>task 2: Create outstanding payments component</name>
  <files>src/components/dashboard/outstanding-list.tsx</files>
  <read_first>
- src/components/payment/payment-list.tsx (from 03-01)
  </read_first>
  <action>
**Create `src/components/dashboard/outstanding-list.tsx`:**

1. Props: userId, showAll (boolean)
2. Fetch pending + overdue payments for user
3. Sort by due date (earliest first)
4. Display as compact list (not full table):
   - Tenant name (or "No tenant")
   - Amount (MYR)
   - Due date
   - Status badge (color-coded per D-34):
     - Yellow (bg-yellow-100): Due within 3 days
     - Orange (bg-orange-100): 1-3 days overdue
     - Red (bg-red-100): 4+ days overdue
5. Actions per item: "Pay Now" button → redirects to payment entry
6. Summary at top: "{N} outstanding payments totaling {AMOUNT}"
7. If showAll=false: Only show first 5, with "View all" link
  </action>
  <verify>
<automated>test -f src/components/dashboard/outstanding-list.tsx</automated>
  </verify>
  <done>
- Outstanding payments displayed with color-coded alerts
- Click "Pay Now" goes to payment entry
  </done>
</task>

<task type="auto">
  <name>task 3: Integrate into dashboard page</name>
  <files>src/app/(dashboard)/page.tsx</files>
  <read_first>
- src/app/(dashboard)/page.tsx (existing - replace placeholders)
- src/components/dashboard/financial-summary.tsx (to integrate)
- src/components/dashboard/income-expense-chart.tsx (to integrate)
- src/components/dashboard/outstanding-list.tsx (to integrate)
  </read_first>
  <action>
**Update `src/app/(dashboard)/page.tsx`:**

1. Add time period filter: This Month | Last Month | This Year | Custom
2. Replace placeholder stats with real data:
   - Properties: fetch from property-service
   - Tenants: fetch from tenant-service
   - Monthly Income: from financial-summary
   - Occupancy Rate: calculate (occupied / total) * 100
3. Add new sections:
   - **Financial Summary Card Row**: Shows 4 summary cards
   - **Chart Section**: Income/expense bar chart
   - **Outstanding Payments Section**: Compact outstanding list with alerts

4. Layout:
   ```
   [Period Filter]
   [Stats Row (Properties, Tenants, Income, Occupancy)]
   [Financial Summary Cards (Income, Expenses, Net, Outstanding)]
   [Chart Section - full width]
   [Outstanding Payments - side or below]
   ```

5. Handle empty state for each section
6. Loading states while data fetches
  </action>
  <verify>
<automated>grep -q "financial-summary" src/app/\(dashboard\)/page.tsx</automated>
  </verify>
  <done>
- Dashboard shows real data from database
- Chart renders on page load
- Outstanding alerts visible
  </done>
</task>

</tasks>

<verification>
- [ ] Chart shows monthly income vs expenses
- [ ] Summary cards show correct totals (verified against raw data)
- [ ] Outstanding alerts color-coded correctly
- [ ] Period filter updates all data
</verification>

<success_criteria>
Dashboard displays income/expense chart, summary cards with totals, and outstanding payments with alerts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-core/03-02-SUMMARY.md`
</output>