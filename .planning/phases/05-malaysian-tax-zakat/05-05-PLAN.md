---
phase: 05-malaysian-tax-zakat
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified: []
autonomous: true
requirements: [TX-01, TX-02, TX-03]
user_setup: []

must_haves:
  truths:
    - "Excel export includes Zakat and Tax columns"
    - "P&L export shows Zakat and Tax breakdown"
    - "Export follows LHDN format with additional columns"
  artifacts:
    - path: "src/services/export-service.ts"
      provides: "Updated export with Zakat/Tax columns"
---

<objective>
Update Excel export to include Zakat and Tax columns.

Purpose: Financial exports include Zakat and Tax calculations (TX-01, TX-02, TX-03)
Output: Updated export service
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/services/export-service.ts
@src/lib/tax-offset.ts
@src/lib/zakat.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Update P&L export with Zakat</name>
  <files>src/services/export-service.ts</files>
  <action>
Update exportProfitLoss function in src/services/export-service.ts:
- Import calculateZakat from @/lib/zakat
- After calculating netProfit, call calculateZakat(netProfit)
- Add to P&L export rows:
  - After "NET PROFIT/(LOSS)" row, add "Zakat Perniagaan" row with amount (or "N/A")
  - Add "Net Profit after Zakat" row: netProfit - Zakat amount
- Use existing formatMYR for currency
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>P&L export includes Zakat</done>
</task>

<task type="auto">
  <name>task 2: Create Tax export format</name>
  <files>src/services/export-service.ts</files>
  <action>
Add new export function exportTaxSummary to src/services/export-service.ts:
- Import calculateTaxWithZakatOffset from @/lib/tax-offset
- Accept year parameter
- Calculate profit/loss for the year
- Call calculateTaxWithZakatOffset with net profit
- Create Excel with columns:
  - Year
  - Net Profit (RM)
  - Zakat Amount (RM)
  - Gross Tax (RM)
  - Zakat Offset (RM)
  - Net Tax Payable (RM)
  - Effective Rate (%)
- Add to ExportFormat type as 'tax-summary'
- Add FORMAT_LABELS entry
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>Tax summary export format created</done>
</task>

<task type="auto">
  <name>task 3: Add Tax Summary to Reports page</name>
  <files>src/components/reports/export-button.tsx</files>
  <action>
Update export-button.tsx:
- Add 'tax-summary' to ExportFormat type
- Add FORMAT_LABELS entry: 'tax-summary': 'Tax Summary'
- Update Reports page to include Tax Summary export option
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>Tax Summary export available in UI</done>
</task>

</tasks>

<verification>
- [ ] npm run typecheck passes
- [ ] P&L export includes Zakat rows
- [ ] New Tax Summary export works
- [ ] Reports page shows Tax Summary option
</verification>

<success_criteria>
- Excel export includes Zakat column in P&L
- Tax Summary export available
- All currency formatted as RM
</success_criteria>

<output>
After completion, create `.planning/phases/05-malaysian-tax-zakat/05-05-SUMMARY.md`
</output>