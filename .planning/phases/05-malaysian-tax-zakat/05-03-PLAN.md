---
phase: 05-malaysian-tax-zakat
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: []
autonomous: true
requirements: [TX-02, TX-03]
user_setup: []

must_haves:
  truths:
    - "Tax is calculated using LHDN progressive brackets"
    - "Zakat can be offset against income tax liability"
    - "Net tax payable = tax - Zakat (cannot go below 0)"
  artifacts:
    - path: "src/lib/tax-offset.ts"
      provides: "Zakat offset calculation"
      exports: ["calculateTaxWithZakatOffset", "TaxWithOffsetResult"]
    - path: "src/components/reports/tax-summary.tsx"
      provides: "Tax calculation UI with Zakat offset"
---

<objective>
Add tax calculation and Zakat offset capability.

Purpose: Implement LHDN tax brackets and Zakat offset against tax (TX-02, TX-03)
Output: Tax utility with offset and UI component
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/tax-calculation.ts (from 05-01)
@src/lib/zakat.ts (from 05-01)
@src/services/profit-loss-service.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Tax offset utility</name>
  <files>src/lib/tax-offset.ts</files>
  <action>
Create src/lib/tax-offset.ts:
- Import calculateTax from @/lib/tax-calculation
- Import calculateZakat from @/lib/zakat
- TaxWithOffsetResult interface: taxableIncome, grossTax, ZakatAmount, ZakatOffset, netTaxPayable, effectiveRate
- calculateTaxWithZakatOffset(netProfitSen: number): TaxWithOffsetResult function that:
  - Calculate Zakat: call calculateZakat(netProfitSen)
  - Calculate Tax: call calculateTax(netProfitSen) - net profit IS taxable income
  - Zakat offset: min(Zakat amount, grossTax) - cannot offset more than tax due
  - Net tax payable: max(0, grossTax - ZakatOffset)
  - Return: taxableIncome (profit), grossTax, ZakatAmount (calculated), ZakatOffset (applied), netTaxPayable, effectiveRate
- Export all via named exports
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>Tax offset utility exported</done>
</task>

<task type="auto">
  <name>task 2: Create Tax Summary component</name>
  <files>src/components/reports/tax-summary.tsx</files>
  <action>
Create src/components/reports/tax-summary.tsx:
- Props: netProfit (number in sen), year (number)
- Use client component ('use client')
- Import calculateTaxWithZakatOffset from @/lib/tax-offset
- Calculate on mount/render: get result from calculateTaxWithZakatOffset(netProfit)
- Display sections:
  1. Net Profit (highlighted)
  2. Tax Calculation: Show bracket breakdown (or summary)
  3. Zakat: Show amount (or "Not applicable")
  4. Zakat Offset: Show offset amount (min of Zakat and tax)
  5. Net Tax Payable: Final amount after offset (highlighted)
  6. Effective Rate: percentage
- Use formatCurrency for amounts
- Style: Clean table/grid layout, clear sections
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>Tax summary component renders</done>
</task>

<task type="auto">
  <name>task 3: Add Tax Summary to Reports page</name>
  <files>src/app/reports/page.tsx</files>
  <action>
Update src/app/reports/page.tsx:
- Import TaxSummary from @/components/reports/tax-summary
- Add "Tax Calculation" section after P&L section
- Pass netProfit from P&L data to TaxSummary component
- Show year selector that syncs with P&L year
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>Reports page displays tax calculation with Zakat offset</done>
</task>

</tasks>

<verification>
- [ ] npm run typecheck passes
- [ ] Tax calculates with LHDN brackets
- [ ] Zakat offsets tax (capped at tax amount)
- [ ] Net tax payable cannot be negative
- [ ] Reports page shows tax summary with offset
</verification>

<success_criteria>
- Tax calculation follows LHDN progressive brackets
- Zakat offset correctly applied
- Effective rate displayed
</success_criteria>

<output>
After completion, create `.planning/phases/05-malaysian-tax-zakat/05-03-SUMMARY.md`
</output>