---
phase: 07
plan: 03
wave: 2
objective: Add dashboard components for YTD summary, property performance, occupancy trends, and cash flow forecast
autonomous: true
files_created:
  - src/components/dashboard/ytd-summary.tsx
  - src/components/dashboard/property-performance.tsx
  - src/components/dashboard/occupancy-trends-chart.tsx
  - src/components/dashboard/cash-flow-forecast.tsx
files_modified:
  - src/app/(dashboard)/page.tsx
tasks: 5
key-links:
  - via: imports
    to: src/services/business-summary-service.ts
    pattern: ytd-stats|property-breakdown
  - via: fetches
    to: src/app/api/v1/reports/ytd-stats/route.ts
    pattern: ytd-stats
  - via: fetches
    to: src/app/api/v1/reports/property-breakdown/route.ts
    pattern: property-breakdown
  - via: fetches
    to: src/app/api/v1/reports/cash-flow-forecast/route.ts
    pattern: cash-flow-forecast
---

## Task 2.1-2.4: Dashboard Components

Add new dashboard components for business analytics.

### Component 2.1: YTD Summary Cards
**File:** `src/components/dashboard/ytd-summary.tsx`

A row of 3 cards showing:
- YTD Income (with YoY % change)
- YTD Expenses (with YoY % change)  
- YTD Net Profit (with YoY % change)

Features:
- Fetch from /api/v1/reports/ytd-stats?userId=demo-user
- Show percentage change with arrow (green up, red down)
- Loading skeleton state
- Empty state (all zeros)

### Component 2.2: Property Performance Section
**File:** `src/components/dashboard/property-performance.tsx`

Expandable section showing property breakdown:
- Toggle button: "By Property" | "By Type"
- Cards or table view
- Each property shows: Name, Type, Status, Income, Expenses, Net Profit
- When grouped by type: show totals per type (condo, apartment, etc.)

Features:
- Fetch from /api/v1/reports/property-breakdown?userId=demo-user
- Collapsible (starts expanded or collapsed based on preference)
- Loading skeleton
- Empty state: "No properties yet"

### Component 2.3: Occupancy Trends Chart
**File:** `src/components/dashboard/occupancy-trends-chart.tsx`

Line chart showing occupancy rate over 12 months:
- X-axis: Months (Jan-Dec)
- Y-axis: 0-100%
- Two lines: Current year (solid), Previous year (dashed)
- Tooltip showing exact percentage
- Legend

Features:
- Calculate occupancy from leases (occupied rooms / total rooms per month)
- Use Recharts (already installed and used in codebase)
- Follow existing chart patterns from income-expense-chart.tsx

### Component 2.4: Cash Flow Forecast Widget
**File:** `src/components/dashboard/cash-flow-forecast.tsx`

Widget showing next 3 months forecast:
- Bar chart or cards
- For each month: Expected Income (green), Expected Expenses (red), Net (blue)
- Summary totals at bottom

Features:
- Fetch from /api/v1/reports/cash-flow-forecast?userId=demo-user
- Use Recharts BarChart or custom cards
- Loading skeleton

### Dashboard Page Update
**File:** `src/app/(dashboard)/page.tsx` (modify)

Add new sections to the dashboard:
- New YTD Summary Row - above existing stats
- Property Performance - below financial summary
- Two-column layout for Occupancy Trends and Cash Flow Forecast

### Implementation Notes
- Follow existing component patterns from src/components/dashboard/
- Use same styling conventions (Tailwind CSS modules)
- Use existing skeleton pattern for loading states
- Use existing currency formatting from @/lib/format
- Keep components under 150 lines each
