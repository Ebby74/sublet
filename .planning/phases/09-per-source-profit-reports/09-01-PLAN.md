---
phase: 09-per-source-profit-reports
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/v1/reports/profit-by-source/route.ts
  - src/services/profit-report-service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can view profit/loss calculated for each income source"
    - "User can filter reports by date range"
    - "API returns revenue, expenses, and net profit per source"
  artifacts:
    - path: "src/app/api/v1/reports/profit-by-source/route.ts"
      provides: "API endpoint for profit-by-source data"
      exports: ["GET"]
    - path: "src/services/profit-report-service.ts"
      provides: "Business logic for profit calculations"
      exports: ["getProfitBySource"]
  key_links:
    - from: "src/app/api/v1/reports/profit-by-source/route.ts"
      to: "src/services/payment-service.ts"
      via: "getPayments with incomeSource filter"
      pattern: "getPayments.*incomeSource"
---

<objective>
Create API endpoint and service for profit-by-source reports.
Calculates revenue - expenses = net profit per income source with date filtering.
</objective>

<read_first>
- src/services/payment-service.ts — Use getPayments with incomeSource filter
- src/lib/income-sources.ts — INCOME_SOURCES constant for labels/colors
- prisma/schema.prisma — Payment model with incomeSource field
</read_first>

<context>
@.planning/phases/09-per-source-profit-reports/09-CONTEXT.md
@src/lib/income-sources.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Create profit report service</name>
  <files>src/services/profit-report-service.ts</files>
  <behavior>
    - getProfitBySource(userId, dateRange?) returns { sublet: {revenue, expenses, profit}, autoren_sell: {...}, autoren_rent: {...} }
    - Filters by date range (startDate, endDate)
    - Calculates: revenue (income payments) - expenses (expense payments) = net profit per source
  </behavior>
  <action>
    Create src/services/profit-report-service.ts with:
    - Interface ProfitBySourceResult: { incomeSource, label, revenue, expenses, profit, margin }
    - Interface DateRange: { startDate, endDate }
    - getProfitBySource(userId: string, dateRange?: DateRange): Promise<ProfitBySourceResult[]>
    - Query income payments: getPayments({ userId, type: 'income', startDate, endDate, incomeSource })
    - Query expense payments: getPayments({ userId, type: 'expense', startDate, endDate, incomeSource })
    - Group by incomeSource, sum amounts
    - Calculate profit = revenue - expenses
    - Calculate margin = (profit / revenue) * 100
    - Return array sorted by profit descending
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    getProfitBySource returns profit data for all income sources with correct calculations
  </done>
</task>

<task type="auto">
  <name>Create profit-by-source API endpoint</name>
  <files>src/app/api/v1/reports/profit-by-source/route.ts</files>
  <action>
    Create GET /api/v1/reports/profit-by-source endpoint:
    - Accept query params: startDate (ISO), endDate (ISO)
    - Parse userId from session or use 'demo-user' for now
    - Call getProfitBySource(userId, { startDate, endDate })
    - Return JSON: { data: ProfitBySourceResult[], meta: { startDate, endDate, generatedAt } }
    Use existing report API patterns from /api/v1/reports/ytd-stats
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    GET /api/v1/reports/profit-by-source returns profit data with date filtering
  </done>
</task>

</tasks>

<verification>
- [ ] profit-report-service.ts exports getProfitBySource
- [ ] API route returns JSON with data and meta
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
API returns profit data for Sublet, Autoren Sell, Autoren Rent with revenue/expenses/net profit breakdown
</success_criteria>

<output>
After completion, create `.planning/phases/09-per-source-profit-reports/09-01-SUMMARY.md`
</output>