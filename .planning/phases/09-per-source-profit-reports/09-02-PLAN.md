---
phase: 09-per-source-profit-reports
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/reports/profit-by-source-report.tsx
  - src/app/reports/profit/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can view profit/loss in table format"
    - "User can see profit summary cards with margin %"
    - "Negative profits shown in red with warning indicator"
  artifacts:
    - path: "src/components/reports/profit-by-source-report.tsx"
      provides: "Profit report UI with table and cards"
      exports: ["ProfitBySourceReport"]
    - path: "src/app/reports/profit/page.tsx"
      provides: "Profit report page route"
      exports: ["default"]
  key_links:
    - from: "src/components/reports/profit-by-source-report.tsx"
      to: "/api/v1/reports/profit-by-source"
      via: "fetch in useEffect"
      pattern: "fetch.*profit-by-source"
---

<objective>
Create frontend profit-by-source report component with table and cards view.
Shows profit/loss per income source with visualizations and date filtering.
</objective>

<read_first>
- src/components/reports/business-summary-report.tsx — Existing report patterns
- src/components/reports/expense-allocation-report.tsx — Phase 08 expense report (similar structure)
- src/components/reports/profit-loss-statement.tsx — Existing P&L pattern
</read_first>

<context>
@.planning/phases/09-per-source-profit-reports/09-CONTEXT.md
@src/components/reports/expense-allocation-report.tsx
</context>

<tasks>

<task type="auto">
  <name>Create ProfitBySourceReport component</name>
  <files>src/components/reports/profit-by-source-report.tsx</files>
  <action>
    Create src/components/reports/profit-by-source-report.tsx:
    - 'use client' directive
    - Interface ProfitData: { incomeSource, label, revenue, expenses, profit, margin, color }
    - State: data[], isLoading, dateRange { startDate, endDate }
    - Fetch from /api/v1/reports/profit-by-source on mount and dateRange change
    - Table view: columns Income Source | Revenue | Expenses | Net Profit | Margin %
    - Cards view: Each source as card with icon, label, profit amount, margin %
    - Date filter: Preset dropdown (This Month, Last Month, This Quarter, Last Quarter, This Year) + custom picker
    - Loss handling (per D-04): If profit < 0, show in red with warning icon
    - Use INCOME_SOURCES colors for each source
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    Component displays profit data in table + cards with colors and loss handling
  </done>
</task>

<task type="auto">
  <name>Create profit report page</name>
  <files>src/app/reports/profit/page.tsx</files>
  <action>
    Create src/app/reports/profit/page.tsx:
    - Import and render ProfitBySourceReport component
    - Page title: "Profit by Source"
    - Use existing page patterns from /app/reports/page.tsx
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
    Profit report page accessible at /reports/profit
  </done>
</task>

</tasks>

<verification>
- [ ] Component renders table and cards with profit data
- [ ] Date filtering works
- [ ] Negative profits shown in red
- [ ] Page compiles and renders
</verification>

<success_criteria>
User can view profit/loss by income source with date filtering and loss highlighting
</success_criteria>

<output>
After completion, create `.planning/phases/09-per-source-profit-reports/09-02-SUMMARY.md`
</output>