---
phase: 10-consolidated-financials
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reports/balance-sheet-report.tsx
  - src/app/api/v1/reports/balance-sheet/route.ts
  - src/services/balance-sheet-service.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can see simple balance sheet with assets, liabilities, equity"
    - "Balance sheet shows: Bank/Cash balance, Accounts Receivable, Accounts Payable, Equity"
    - "Equity calculation: Opening balance + Net Profit - Drawings"
  artifacts:
    - path: "src/components/reports/balance-sheet-report.tsx"
      provides: "Balance Sheet UI component"
      min_lines: 100
    - path: "src/app/api/v1/reports/balance-sheet/route.ts"
      provides: "API endpoint for balance sheet data"
      exports: ["GET"]
    - path: "src/services/balance-sheet-service.ts"
      provides: "Service to calculate assets, liabilities, equity"
      exports: ["getBalanceSheet"]
  key_links:
    - from: "balance-sheet-report.tsx"
      to: "/api/v1/reports/balance-sheet"
      via: "fetch in useEffect"
      pattern: "fetch.*balance-sheet"
    - from: "balance-sheet-service.ts"
      to: "prisma.payment"
      via: "aggregate queries for receivables/payables"
      pattern: "prisma\\.payment\\.aggregate"
---

<objective>
Create simple Balance Sheet component for small agency. Shows Assets (Bank/Cash, Accounts Receivable), Liabilities (Accounts Payable), and Equity (Opening balance + Net Profit - Drawings).

Purpose: Complete financial picture alongside P&L.
Output: Working Balance Sheet with API endpoint and service layer.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-consolidated-financials/10-CONTEXT.md
@src/components/reports/profit-by-source-report.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>task 1: Create balance sheet service</name>
  <files>src/services/balance-sheet-service.ts</files>
  <action>
Create src/services/balance-sheet-service.ts:
1. Import prisma from @/lib/prisma
2. Export interface BalanceSheet { assets: { cash: number, receivables: number }, liabilities: { payables: number }, equity: { opening: number, netProfit: number, drawings: number }, total: { assets: number, liabilities: number, equity: number } }
3. Create getBalanceSheet(userId, dateRange?) function that:
   - Cash: Sum of all 'paid' income payments (money received)
   - Receivables: Sum of all 'pending' or 'overdue' income payments (money owed)
   - Payables: Sum of all 'pending' or 'overdue' expense payments (money owed)
   - Opening balance: Start with 0 (or configurable)
   - Net Profit: Call getProfitBySource and sum all profits
   - Drawings: Could be tracked as negative payments or configurable
4. Return balance sheet object with totals calculated
5. Use DateRange from profit-report-service for optional period filter

Note: For simplicity, track cash as total income received (not actual bank balance). This is "sources and uses" style balance sheet appropriate for small agency.
  </action>
  <verify>
<automated>grep -l "getBalanceSheet" src/services/balance-sheet-service.ts</automated>
  </verify>
  <done>Service exports getBalanceSheet function with assets/liabilities/equity structure</done>
</task>

<task type="auto">
  <name>task 2: Create balance sheet API route</name>
  <files>src/app/api/v1/reports/balance-sheet/route.ts</files>
  <action>
Create src/app/api/v1/reports/balance-sheet/route.ts:
1. GET handler accepting userId query param and optional startDate/endDate
2. Calls getBalanceSheet(userId, dateRange) from balance-sheet-service
3. Returns { data: {...}, meta: { asOf, generatedAt }, error: null }
4. Follows existing API pattern from profit-by-source/route.ts

Use x-user-id header for auth. Return 400 if userId missing.
  </action>
  <verify>
<automated>ls src/app/api/v1/reports/balance-sheet/route.ts</automated>
  </verify>
  <done>API route returns balance sheet data with assets, liabilities, equity</done>
</task>

<task type="auto">
  <name>task 3: Create balance sheet component</name>
  <files>src/components/reports/balance-sheet-report.tsx</files>
  <action>
Create src/components/reports/balance-sheet-report.tsx:
1. Use 'use client' directive
2. Create simple Balance Sheet layout per D-02:
   - LEFT/CENTER: Assets section with Bank/Cash + Accounts Receivable cards
   - RIGHT/CENTER: Liabilities section with Accounts Payable card
   - BOTTOM: Equity section showing Opening + Net Profit - Drawings
   - Totals row at bottom (Assets = Liabilities + Equity)
3. Use formatCurrency from @/lib/format for MYR display
4. Period selector: This Month, Last Month, This Quarter, Last Quarter, This Year, Last Year
5. Fetch from /api/v1/reports/balance-sheet endpoint
6. Show loading skeleton during fetch (reuse pattern)
7. Add empty state: "No transactions yet" with guidance to add income/expenses

Component name: BalanceSheetReport({ userId?: string })
  </action>
  <verify>
<automated>grep -l "BalanceSheetReport" src/components/reports/balance-sheet-report.tsx</automated>
  </verify>
  <done>Balance Sheet with assets/liabilities/equity displayed correctly</done>
</task>

</tasks>

<verification>
- Balance Sheet renders with Assets, Liabilities, Equity sections
- Period selection updates the data
- Totals calculate correctly (Assets = Liabilities + Equity)
- Empty state shows when no data
</verification>

<success_criteria>
User can view simple Balance Sheet with cash, receivables, payables, and equity.
</success_criteria>

<output>
After completion, create `.planning/phases/10-consolidated-financials/10-02-SUMMARY.md`
</output>