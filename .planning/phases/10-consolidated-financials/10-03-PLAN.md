---
phase: 10-consolidated-financials
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - src/components/dashboard/consolidated-summary-widget.tsx
  - src/app/reports/consolidated/page.tsx
  - src/app/api/v1/reports/consolidated/export/route.ts
  - src/services/export-service.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dashboard shows consolidated financial summary widgets (Revenue, Expenses, Net Profit, Margin %)"
    - "Mini bar chart shows 6-month trend on dashboard"
    - "Full consolidated report page at /reports/consolidated"
    - "Export to PDF and Excel formats available"
  artifacts:
    - path: "src/components/dashboard/consolidated-summary-widget.tsx"
      provides: "Dashboard summary cards with key metrics"
      min_lines: 60
    - path: "src/app/reports/consolidated/page.tsx"
      provides: "Full consolidated reports page"
      min_lines: 50
    - path: "src/app/api/v1/reports/consolidated/export/route.ts"
      provides: "Export API for PDF/Excel download"
      exports: ["POST"]
  key_links:
    - from: "consolidated-summary-widget.tsx"
      to: "/api/v1/reports/consolidated-pl"
      via: "fetch for summary data"
      pattern: "fetch.*consolidated-pl"
    - from: "page.tsx"
      to: "consolidated-pl-report.tsx"
      via: "import and render component"
      pattern: "ConsolidatedPLReport"
---

<objective>
Add dashboard summary widgets with key metrics and mini trend chart, plus consolidated report page with PDF/Excel export functionality.

Purpose: Quick financial snapshot on dashboard + full report access with export.
Output: Dashboard widgets, consolidated reports page, export functionality.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-consolidated-financials/10-CONTEXT.md
@src/components/reports/profit-by-source-report.tsx
@src/components/reports/export-button.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Create dashboard summary widget</name>
  <files>src/components/dashboard/consolidated-summary-widget.tsx</files>
  <action>
Create src/components/dashboard/consolidated-summary-widget.tsx:
1. Use 'use client' directive
2. Create 4 metric cards: Total Revenue, Total Expenses, Net Profit, Profit Margin %
3. Each card: icon (DollarSign, TrendingUp, Building2, Percent), label, value in MYR format
4. Use formatCurrency from @/lib/format
5. Fetch current month data from /api/v1/reports/consolidated-pl for this month
6. Add mini bar chart (6-month trend) using Recharts - reuse pattern from profit-by-source-report.tsx
7. Loading state: show skeleton cards
8. Empty state: "No transactions this month" message

Component name: ConsolidatedSummaryWidget({ userId?: string })
  </action>
  <verify>
<automated>grep -l "ConsolidatedSummaryWidget" src/components/dashboard/consolidated-summary-widget.tsx</automated>
  </verify>
  <done>Dashboard widget with 4 metric cards and 6-month mini trend chart</done>
</task>

<task type="auto">
  <name>task 2: Create consolidated reports page</name>
  <files>src/app/reports/consolidated/page.tsx</files>
  <action>
Create src/app/reports/consolidated/page.tsx:
1. Standard page layout with header and title "Consolidated Financials"
2. Import and render ConsolidatedPLReport and BalanceSheetReport components
3. Layout: P&L on top, Balance Sheet below
4. Add export buttons: "Export PDF" and "Export Excel" (reuse ExportButton pattern)
5. Period selector shared between both reports
6. Use same styling as other report pages

Route: /reports/consolidated
  </action>
  <verify>
<automated>ls src/app/reports/consolidated/page.tsx</automated>
  </verify>
  <done>Full consolidated reports page with P&L and Balance Sheet</done>
</task>

<task type="auto">
<parameter name>task 3: Add consolidated export to export service</name>
<files>src/services/export-service.ts</files>
 <action>
Update src/services/export-service.ts:
1. Add new export function: exportConsolidatedReport(data, format)
2. For Excel: Create multi-sheet workbook:
   - Sheet 1: P&L Summary (revenue, expenses, profit per source + totals)
   - Sheet 2: Balance Sheet (assets, liabilities, equity, totals)
   - Sheet 3: Summary (period, generated date, key metrics)
3. For PDF: Use browser print-to-PDF (single page summary, reuse pattern from Phase 04)
4. Update export API route to handle new format 'consolidated'
5. Follow existing export patterns (createWorksheet, styleWorksheet)

Import XLSX from 'xlsx' for Excel export.
  </action>
  <verify>
<automated>grep -l "exportConsolidatedReport" src/services/export-service.ts</automated>
  </verify>
  <done>Export service supports consolidated report in PDF and Excel formats</done>
</task>

</tasks>

<verification>
- Dashboard shows consolidated summary with 4 metrics and mini chart
- /reports/consolidated page renders with both P&L and Balance Sheet
- Export buttons generate downloadable files
- PDF is print-friendly single page
- Excel has multiple sheets with full data
</verification>

<success_criteria>
User can view consolidated financials on dashboard with quick metrics and access full reports page with export options.
</success_criteria>

<output>
After completion, create `.planning/phases/10-consolidated-financials/10-03-SUMMARY.md`
</output>