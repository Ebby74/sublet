---
phase: 11-ssm-export-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/export-service.ts
  - src/app/api/v1/export/route.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can export Form 9 (Return of Allotment of Shares) as Excel"
    - "User can export Form 44 (Statement of Affairs) as Excel"
    - "Exports contain company info and director signatures section"
    - "Exports use multiple sheets matching SSM form sections"
  artifacts:
    - path: "src/services/export-service.ts"
      provides: "exportSSMForm9 and exportSSMForm44 functions"
      exports: ["exportSSMForm9", "exportSSMForm44"]
    - path: "src/app/api/v1/export/route.ts"
      provides: "SSM export API endpoints"
      adds_formats: ["ssm-form9", "ssm-form44"]
  key_links:
    - from: "src/app/api/v1/export/route.ts"
      to: "src/services/export-service.ts"
      via: "exportSSMForm9/exportSSMForm44 calls"
---

<objective>
Create SSM export service functions for Form 9 and Form 44, plus API routes to serve them.

Purpose: Add SSM-compliant Excel export formats using existing xlsx patterns
Output: Two new export functions and API endpoints
</objective>

<context>
@src/services/export-service.ts (Existing export patterns - follow createWorksheet, styleWorksheet, multi-sheet patterns)
@src/app/api/v1/export/route.ts (Export API route - add new formats here)
@src/components/reports/consolidated-pl-report.tsx (P&L data structure for Form 44)
@src/components/reports/balance-sheet-report.tsx (Balance Sheet data for Form 44)

**SSM Form Requirements (D-01, D-02, D-03):**
- Form 9: Return of Allotment of Shares
- Form 44: Statement of Affairs
- Multiple sheets matching SSM form sections
- Both P&L and Balance Sheet data per SSM format
- Company name, registration number, director signatures section
</context>

<tasks>

<task type="auto">
  <name>task 1: Add SSM export types and interfaces</name>
  <files>src/services/export-service.ts</files>
  <read_first>
    - src/services/export-service.ts (lines 1-60 for type patterns, 1100-1153 for helper functions)
  </read_first>
  <action>
Add to the ExportFormat type union at line 32:
- 'ssm-form9'
- 'ssm-form44'

Add SSM-specific type definitions:
```typescript
interface SSMCompanyInfo {
  companyName: string;
  registrationNumber: string;
  incorporationDate: string;
  registeredAddress: string;
  businessAddress: string;
}

interface SSMDirectorSignature {
  name: string;
  designation: string;
  icNumber: string;
  date: string;
}
```

Add formatMYR function for RM amounts (already exists, reuse formatMYR(sen: number)).

Use existing XLSX library already imported at line 14.
  </action>
  <verify>
    <automated>grep -n "ssm-form9\|ssm-form44" src/services/export-service.ts | head -5</automated>
  </verify>
  <done>ExportFormat type includes ssm-form9 and ssm-form44</done>
</task>

<task type="auto">
  <name>task 2: Implement exportSSMForm9 function</name>
  <files>src/services/export-service.ts</files>
  <read_first>
    - src/services/export-service.ts (lines 919-1087 for exportConsolidatedReport as multi-sheet pattern)
  </read_first>
  <action>
Add exportSSMForm9 function that creates Form 9 (Return of Allotment of Shares):

Function signature:
```typescript
export async function exportSSMForm9(userId: string): Promise<XLSX.WorkBook>
```

Sheet structure:
1. **"Cover"** - Title page with:
   - "FORM 9" header
   - "RETURN OF ALLOTMENT OF SHARES"
   - Company Name (from company config or "AMR Home Solutions")
   - Registration Number
   - Date of Return
   
2. **"Allotment Summary"** - Overview:
   - Total Shares Allotted (from payments/leases)
   - Share Capital
   - Allotment Date

3. **"Share Allotments"** - Detailed table:
   - Date of Allotment
   - Number of Shares
   - Class of Shares
   - Nominal Value (RM)
   - Amount Paid (RM)
   - Allottee Name
   - Allottee IC Number

Use existing patterns:
- formatMYR() for currency
- createWorksheet() for single sheets
- XLSX.utils.book_new() and XLSX.utils.book_append_sheet() for multi-sheet
- styleWorksheet() for header styling

Data source: Use prisma to fetch lease/payment data for sublet business unit as share allotments.
  </action>
  <verify>
    <automated>grep -n "exportSSMForm9" src/services/export-service.ts | head -3</automated>
  </verify>
  <done>exportSSMForm9 function returns XLSX.WorkBook with Cover, Allotment Summary, and Share Allotments sheets</done>
</task>

<task type="auto">
  <name>task 3: Implement exportSSMForm44 function</name>
  <files>src/services/export-service.ts</files>
  <read_first>
    - src/services/export-service.ts (lines 919-1087 for exportConsolidatedReport pattern)
    - src/components/reports/consolidated-pl-report.tsx (for P&L data structure)
    - src/components/reports/balance-sheet-report.tsx (for Balance Sheet data structure)
  </read_first>
  <action>
Add exportSSMForm44 function that creates Form 44 (Statement of Affairs):

Function signature:
```typescript
export async function exportSSMForm44(userId: string): Promise<XLSX.WorkBook>
```

Sheet structure:
1. **"Cover"** - Title page with:
   - "FORM 44" header
   - "STATEMENT OF AFFAIRS"
   - Company Name
   - Registration Number
   - Date of Statement
   - "In accordance with Section 167 of the Companies Act 2016"

2. **"Profit & Loss"** - P&L Statement:
   - Heading: "PROFIT AND LOSS ACCOUNT"
   - Trading Account section
   - Income breakdown by category
   - Expenses breakdown by category
   - Net Profit/Loss
   - Use consolidated P&L data

3. **"Balance Sheet"** - Statement of Affairs:
   - Heading: "BALANCE SHEET"
   - ASSETS section (with sub-total)
   - LIABILITIES section (with sub-total)
   - EQUITY section (share capital + reserves + retained earnings)
   - Must balance: Total Assets = Total Liabilities + Equity

4. **"Notes"** - Supporting information:
   - Accounting policies
   - Basis of preparation
   - Director signatures section

Use existing patterns from exportConsolidatedReport and BalanceSheet calculations.
Fetch data from existing API endpoints or replicate the logic directly.
  </action>
  <verify>
    <automated>grep -n "exportSSMForm44" src/services/export-service.ts | head -3</automated>
  </verify>
  <done>exportSSMForm44 function returns XLSX.WorkBook with Cover, P&L, Balance Sheet, and Notes sheets</done>
</task>

<task type="auto">
  <name>task 4: Add API endpoints for SSM exports</name>
  <files>src/app/api/v1/export/route.ts</files>
  <read_first>
    - src/app/api/v1/export/route.ts (full file for export routing patterns)
  </read_first>
  <action>
Update the export route:

1. Add to SUPPORTED_FORMATS array:
```typescript
'ssm-form9',
'ssm-form44',
```

2. Import new functions (add to existing import from line 18-36):
```typescript
exportSSMForm9,
exportSSMForm44,
```

3. Add cases to switch statement (after line 200):
```typescript
case 'ssm-form9':
  workbook = await exportSSMForm9(userId);
  filename = `ssm-form-9-${now.toISOString().split('T')[0]}.xlsx`;
  break;

case 'ssm-form44':
  workbook = await exportSSMForm44(userId);
  filename = `ssm-form-44-${now.toISOString().split('T')[0]}.xlsx`;
  break;
```

Follow existing case pattern exactly (line 145-200).
  </action>
  <verify>
    <automated>grep -n "ssm-form9\|ssm-form44" src/app/api/v1/export/route.ts</automated>
  </verify>
  <done>API route handles ssm-form9 and ssm-form44 formats with correct filenames</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. `grep -n "exportSSMForm9\|exportSSMForm44" src/services/export-service.ts` returns 2 functions
3. `grep -n "ssm-form9\|ssm-form44" src/app/api/v1/export/route.ts` returns support and cases
</verification>

<success_criteria>
- exportSSMForm9 function creates 3-sheet workbook (Cover, Summary, Allotments)
- exportSSMForm44 function creates 4-sheet workbook (Cover, P&L, Balance Sheet, Notes)
- API route serves both formats with correct Content-Type and filename
- Functions follow existing xlsx patterns from export-service.ts
</success_criteria>

<output>
After completion, create `.planning/phases/11-ssm-export-templates/11-01-SUMMARY.md`
</output>
